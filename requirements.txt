# ==============================================================================
# DogClaw — Python Dependencies
# Requirements for single-machine deployment
# Install: pip install -r requirements.txt
# ==============================================================================

# ------------------------------------------------------------------------------
# Web Framework & Server
# ------------------------------------------------------------------------------
fastapi==0.111.1
uvicorn[standard]==0.30.3
websockets==12.0
python-multipart==0.0.9          # Form data parsing (file uploads, config)
httpx==0.27.0                    # Async HTTP client for outbound webhook calls
aiohttp==3.9.5                   # Async HTTP client for integration pollers
aiofiles==23.2.1                 # Async file I/O (log tailing, config watching)

# ------------------------------------------------------------------------------
# Data Validation & Serialization
# ------------------------------------------------------------------------------
pydantic==2.7.4
pydantic-settings==2.3.3         # Settings management from env vars / .env files
python-dateutil==2.9.0           # Robust timestamp parsing across log formats
orjson==3.10.5                   # Fast JSON (de)serialization for high-volume events

# ------------------------------------------------------------------------------
# Database & Storage
# ------------------------------------------------------------------------------
sqlalchemy[asyncio]==2.0.31      # ORM with async support
aiosqlite==0.20.0                # Async SQLite driver (default single-machine DB)
alembic==1.13.2                  # Database migrations

# Optional: uncomment for TimescaleDB / PostgreSQL production backend
# asyncpg==0.29.0                # Async PostgreSQL driver
# psycopg2-binary==2.9.9         # Sync PostgreSQL driver (Alembic migrations)

# Optional: uncomment for Redis caching / pub-sub
# redis[hiredis]==5.0.7
# aioredis==2.0.1

# ------------------------------------------------------------------------------
# Authentication & Security
# ------------------------------------------------------------------------------
python-jose[cryptography]==3.3.0  # JWT token creation & validation
passlib[bcrypt]==1.7.4            # Password hashing (bcrypt)
cryptography==42.0.8              # TLS, encryption primitives
secrets                           # stdlib — random token generation (built-in)

# ------------------------------------------------------------------------------
# Configuration & Parsing
# ------------------------------------------------------------------------------
pyyaml==6.0.1                    # Rule & integration config parsing
python-dotenv==1.0.1             # .env file support for secrets
watchdog==4.0.1                  # File system watcher for hot-reloading rules
toml==0.10.2                     # TOML config format support (optional)
jinja2==3.1.4                    # Template engine for notification messages

# ------------------------------------------------------------------------------
# Data Processing & Analysis
# ------------------------------------------------------------------------------
numpy==1.26.4                    # Numerical operations (WatchClaw baselines)
pandas==2.2.2                    # Time-series aggregation, metric processing
scipy==1.13.1                    # Statistical functions (Z-score, decomposition)
statsmodels==0.14.2              # Seasonal decomposition for anomaly detection

# ------------------------------------------------------------------------------
# Machine Learning (WatchClaw Anomaly Detection)
# ------------------------------------------------------------------------------
scikit-learn==1.5.0              # IsolationForest, DBSCAN, StandardScaler

# ------------------------------------------------------------------------------
# AI / LLM Integration
# ------------------------------------------------------------------------------
anthropic==0.28.1                # DogClaw AI Agent (Claude)
openai==1.35.7                   # Optional: OpenAI as LLM provider
tiktoken==0.7.0                  # Token counting for context window management

# Optional: Local LLM via Ollama (no API key needed, air-gapped)
# ollama==0.2.1

# ------------------------------------------------------------------------------
# Threat Intelligence & Enrichment
# ------------------------------------------------------------------------------
geoip2==4.8.0                    # MaxMind GeoLite2 GeoIP lookups
maxminddb==2.6.1                 # MaxMind DB file reader
ipaddress                        # stdlib — IP classification (built-in)
stix2==3.0.1                     # STIX 2.1 threat intel format parsing

# ------------------------------------------------------------------------------
# Cloud Provider SDKs
# ------------------------------------------------------------------------------

# AWS
boto3==1.34.131                  # AWS CloudTrail, GuardDuty, CloudWatch, S3, IAM
botocore==1.34.131               # AWS core (boto3 dependency)

# GCP (optional — uncomment to enable)
# google-cloud-logging==3.10.1
# google-cloud-pubsub==2.21.4
# google-cloud-securitycenter==1.29.0

# Azure (optional — uncomment to enable)
# azure-monitor-query==1.3.0
# azure-identity==1.17.1
# msgraph-sdk==1.4.0

# ------------------------------------------------------------------------------
# Kubernetes & Container Monitoring
# ------------------------------------------------------------------------------
kubernetes==30.1.0               # Kubernetes API client (events, metrics, pods)
docker==7.1.0                    # Docker Engine API client

# ------------------------------------------------------------------------------
# Identity Provider Integrations
# ------------------------------------------------------------------------------
okta==2.9.7                      # Okta System Log API

# ------------------------------------------------------------------------------
# Observability & Tracing (OTLP Receiver)
# ------------------------------------------------------------------------------
opentelemetry-sdk==1.25.0
opentelemetry-api==1.25.0
opentelemetry-proto==1.25.0
opentelemetry-exporter-otlp-proto-grpc==1.25.0
grpcio==1.64.1                   # gRPC for OTLP receiver
grpcio-tools==1.64.1             # Protobuf compilation tools
protobuf==4.25.3                 # Protocol Buffers

# ------------------------------------------------------------------------------
# Log & Metric Ingest Protocols
# ------------------------------------------------------------------------------
confluent-kafka==2.4.0           # Kafka consumer for log/event streams
statsd==4.0.1                    # StatsD metric format parsing
prometheus-client==0.20.0        # Prometheus metrics exposition (for self-monitoring)

# ------------------------------------------------------------------------------
# Notification & Response Integrations
# ------------------------------------------------------------------------------
slack-sdk==3.29.0                # Slack notifications & response bot
pdpyras==5.2.0                   # PagerDuty Events API v2
jira==3.8.0                      # Jira ticket creation for investigations
pygithub==2.3.0                  # GitHub PR review & webhook handling
aiosmtplib==3.0.1                # Async SMTP for email notifications

# ------------------------------------------------------------------------------
# Detection Rule Support
# ------------------------------------------------------------------------------
sigma-cli==0.9.3                 # SIGMA rule parsing and compilation
pySigma==0.11.14                 # SIGMA rule library
cel-python==0.1.5                # Common Expression Language for rule conditions
jsonpath-ng==1.6.1               # JSONPath queries in rule conditions

# ------------------------------------------------------------------------------
# Utilities
# ------------------------------------------------------------------------------
rich==13.7.1                     # Terminal formatting for CLI output
typer==0.12.3                    # CLI interface for management commands
click==8.1.7                     # CLI framework (typer dependency)
structlog==24.2.0                # Structured logging
colorama==0.4.6                  # Cross-platform terminal color support
humanize==4.9.0                  # Human-readable numbers/times in output
arrow==1.3.0                     # Friendly datetime handling
tenacity==8.3.0                  # Retry logic for external API calls
cachetools==5.3.3                # TTLCache for enrichment lookup caching
apscheduler==3.10.4              # Scheduled tasks (WatchClaw runs, cleanup jobs)

# ------------------------------------------------------------------------------
# Development & Testing (install separately with pip install -r requirements-dev.txt)
# Included here commented out for reference
# ------------------------------------------------------------------------------
# pytest==8.2.2
# pytest-asyncio==0.23.7
# pytest-cov==5.0.0
# pytest-httpx==0.30.0
# httpx==0.27.0                  # Already in main deps
# black==24.4.2
# isort==5.13.2
# mypy==1.10.0
# ruff==0.4.9
# locust==2.29.1
# pre-commit==3.7.1
# faker==25.8.0                  # Test data generation
